Readme

# Usage
- The project accepts `.stl` files encoded as ASCII. These must be placed in the `src/assets/models` directory
- The `src/renderer/Main.java` file specifies an `inputFile` variable of type `String` which is then used to reference the file to be rendered.
- Start the program by running the `Main.java` file